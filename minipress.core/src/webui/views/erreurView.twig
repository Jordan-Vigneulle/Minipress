{% extends "pageSquelette.twig" %}

{% block head %}
    <title>Erreur {{ code }}</title>
{% endblock %}

{% block content %}
    <div class="error-container">
        <h2>Erreur {{ code }}</h2>
        <p>{{ message }}</p>
        <a href="{{ url_for('home') }}">Retour à l'accueil</a>
    </div>
{% endblock %}